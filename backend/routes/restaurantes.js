const express = require('express');//tabNIne
const router = express.Router();
const mysqlConnection =require('../db/db');


/// contenido

module.exports = router;

//crear los otros metodos, y estoy probando el repocitorio